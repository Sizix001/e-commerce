<form method="post" action="{{ path('frais_port_delete', {'id': frais_port.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ frais_port.id) }}">
    <button class="btn">Delete</button>
</form>
