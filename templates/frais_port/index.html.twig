{% extends 'base.html.twig' %}

{% block title %}FraisPort index{% endblock %}

{% block body %}
    <h1>FraisPort index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Country</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for frais_port in frais_ports %}
            <tr>
                <td>{{ frais_port.id }}</td>
                <td>{{ frais_port.country }}</td>
                <td>{{ frais_port.price }} €</td>
                <td>
                    <a href="{{ path('frais_port_show', {'id': frais_port.id}) }}">show</a>
                    <a href="{{ path('frais_port_edit', {'id': frais_port.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('frais_port_new') }}">Create new</a>
{% endblock %}
