{% extends 'base.html.twig' %}

{% block title %}FraisPort{% endblock %}

{% block body %}
    <h1>FraisPort</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ frais_port.id }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ frais_port.country }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ frais_port.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('frais_port_index') }}">back to list</a>

    <a href="{{ path('frais_port_edit', {'id': frais_port.id}) }}">edit</a>

    {{ include('frais_port/_delete_form.html.twig') }}
{% endblock %}
